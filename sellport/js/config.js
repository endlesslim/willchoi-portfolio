/* ===========================================================================
   사이트 설정 — 운영(라이브) 전환용
   ---------------------------------------------------------------------------
   ▶ 견적 폼이 어디로 리드를 보낼지 정합니다.

   - 로컬에서 테스트(node server.js)할 때: 아래를 비워두면 서버(/api/quote)로 갑니다.
   - 인터넷에 띄운 뒤(정적 호스팅): 구글 Apps Script 웹앱 URL을 따옴표 안에 붙여넣으세요.
       예) quoteEndpoint: "https://script.google.com/macros/s/AKfy...../exec"
     → 이렇게 하면 폼 제출이 구글 시트 + 이메일 + 카톡/슬랙으로 자동 전송됩니다.

   URL 만드는 법: apps_script/SETUP.md 또는 GO-LIVE.md 참고.
   =========================================================================== */
window.SITE_CONFIG = {
  quoteEndpoint: "", // 운영 전환 시 여기에 Apps Script 웹앱 URL 붙여넣기
};
